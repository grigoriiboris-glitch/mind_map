ChatGPT сказал:

Вот подробная дорожная карта подготовки к собеседованиям с уклоном в Go, разбитая по всем секциям, где Go — ключевая платформа. Эта структура основана на статье с Habr и дополнена практическими материалами и советами.
⚙️ 1. Платформенная секция (Go)
📌 Что проверяют:

    Умение писать надежный, читаемый и эффективный код.

    Знание внутреннего устройства Go (runtime, GC, goroutines).

    Понимание идиом и паттернов Go.

    Умение профилировать и оптимизировать код.

    Работа с конкурентностью и каналами.

📘 Темы, которые нужно покрыть:
✅ 1. Основы языка Go

    Типы данных: int, string, struct, interface, map, slice, chan.

    Работа с указателями (но без pointer arithmetic).

    Методы, интерфейсы, композиция.

    Error handling (включая errors.Is, errors.As, fmt.Errorf с %w).

    Defer, panic, recover.

    Работа с пакетами, модулями, go.mod.

Рекомендуется:

    Go Tour: https://go.dev/tour/

    Книга “The Go Programming Language” (Donovan & Kernighan)

✅ 2. Устройство стандартных структур

    Как устроены:

        slice (что такое capacity, как работает append).

        map (хеш-таблица, коллизии, порядок обхода).

        string (иммутабельность, байты и руны).

    Что спрашивают:

        Почему append может изменить поведение кода?

        Почему range по map не гарантирует порядок?

        Как устроена zero value у map/slice?

Полезное:

    Исходники стандартной библиотеки: src/runtime, src/builtin.

✅ 3. Работа с конкурентностью

    Goroutines: как запускаются, сколько памяти едят, когда завершаются.

    Каналы: chan T, select, buffered/unbuffered, закрытие каналов.

    Паттерны:

        Fan-in / Fan-out.

        Worker Pool.

        Pipeline.

        Context cancellation.

    Семафоры на базе каналов.

    Race conditions и как их искать (go run -race).

Углублённое:

    Go Concurrency Patterns — Rob Pike (видео)

    Go by Example: Channels & Concurrency

✅ 4. Синхронизация и пакет sync

    sync.Mutex, sync.RWMutex

    sync.WaitGroup

    sync.Once, sync.Cond

    sync.Map — когда использовать.

Задачи:

    Написать ограниченный по параллелизму загрузчик.

    Написать safe-счётчик.

    Переход от Mutex к каналам (и наоборот).

✅ 5. Работа с временем и таймаутами

    time.After, time.Ticker, time.Sleep

    context.WithTimeout, context.WithDeadline, context.WithCancel

Ключевое:

    Уметь правильно отменять goroutines.

    Не допускать утечек: “goroutine leak” — частый вопрос.

✅ 6. Архитектура приложений

    Чистая архитектура (Clean Architecture)

    DI (вручную и с помощью fx/wire)

    Варианты организации пакетов (feature-based, layered и др.)

Полезное:

    golang-standards/project-layout

    uber-go/fx (DI), google/wire

✅ 7. Работа с сетью

    net/http, middleware, хендлеры.

    context.Context в HTTP-запросах.

    gRPC + protobuf.

    Мидлвари для логирования, трейсинга, метрик.

✅ 8. Инструменты и DevOps

    Линтеры: golangci-lint, staticcheck.

    Форматирование: gofmt, goimports.

    Тестирование: testing, httptest, gomock, testify.

    Профилирование и трассировка:

        pprof

        trace

        expvar

🧠 Что могут спросить на интервью:
Вопрос	Что проверяет
Чем slice отличается от array?	Знание базовых структур
Что произойдёт при append() к слайсу?	Понимание капасити
Как работает сборщик мусора в Go?	Знание рантайма
Как избежать утечек goroutines?	Работа с context и каналами
Как устроен sync.WaitGroup?	Знание внутренних механизмов
Как сделать Rate Limiter?	Применение таймеров, каналов, sync
🔧 Практика

    Решать Go-задачи на:

        LeetCode с выборкой по Go

        Exercism Go track

        Codeforces с Go (сложнее)

    Мок-собеседования:

        https://interviewing.io/

        Смотреть/проводить разборы на YouTube (по Go собеседованиям)

💡 Советы

    Пишите много кода — заводите pet-проекты.

    Используйте pprof и race detector с первых дней.

    Постоянно применяйте context — это “go-way”.

    Не бойтесь использовать каналы, но не переусложняйте.